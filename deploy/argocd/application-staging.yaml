---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: workout-logger-staging
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/vvojtisek/workout-logger.git
    targetRevision: main
    path: helm/workout-logger
    helm:
      releaseName: workout-logger
      valueFiles:
        - values.yaml
        - values-staging.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
