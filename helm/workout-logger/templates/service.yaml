apiVersion: v1
kind: Service
metadata:
  name: {{ include "workout-logger.fullname" . }}
  labels:
    {{- include "workout-logger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "workout-logger.webLabels" . | nindent 4 }}
